apiVersion: v1
kind: Service
metadata:
  name: sound-recorder
  labels:
    app: sound-recorder
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: sound-recorder
